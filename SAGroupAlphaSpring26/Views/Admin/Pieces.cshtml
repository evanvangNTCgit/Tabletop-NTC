@model IEnumerable<Piece>
<div>
    <p>
        <a asp-controller="Admin" asp-action="AddPiece" class="btn-lg btn btn-outline-dark" style="background-color: mediumpurple" role="button">Add a piece</a> |
        <a asp-controller="Admin" asp-action="RecoverPiece" class="btn-lg btn btn-outline-dark" style="background-color: grey" role="button">Recover a piece</a>
    </p>
    <table class="table saBackground">
        <tr>
            <th>Name</th>
            <th>Actions</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr class="saBackground">
                <td>@item.Name</td>
                <td>
                    <!-- Add the ID onto the route of edit piece so admin controller can pull up from DB what Piece Type to edit. -->
                    <a asp-controller="admin" asp-action="editpiece" asp-route-id="@item.Id" class="admin-edit-btn">Edit</a> |
                    <a asp-controller="admin" asp-action="deletepiececonfirmation" asp-route-id="@item.Id" class="admin-delete-btn">Delete</a>
                </td>
            </tr>
        }
    </table>
</div>